version: '3.3'

services:
    api:
      build:
        context: ./SGGC - webapp
        dockerfile: Dockerfile
      ports:
        - 8080:8080

    ui:
      image: tobypeel/steam_group_game_checker_ui
      depends_on:
        - api
      ports:
         - 3000:3000
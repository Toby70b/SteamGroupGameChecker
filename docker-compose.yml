version: '3.3'

services:
    api:
      build:
        context: ./SGGC - webapp
        dockerfile: Dockerfile
      ports:
        - 8080:8080

    ui:
      build:
        context: ./SGGC - frontend
        dockerfile: Dockerfile
      depends_on:
        - api
      ports:
         - 3000:3000
apiVersion: v1
kind: Service
metadata:
  name: sggc-webservice-svc
  labels:
    app : webservice
spec:
  type: NodePort
  ports:
    - port: 8080
      nodePort: 30001
      protocol: TCP
  selector:
    app : webservice